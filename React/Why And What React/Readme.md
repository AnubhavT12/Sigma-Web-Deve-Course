- we can use react which means that once we update the state variable, it changes across the page

- we cansplit our app into multiple components andreuse those components

- React uses a virtual DOM To efficiently update the UI which is better than updating content using DOM manipulation

- Debugging And MAintainence IS Easy, select test and review components individually